/* 
DATA and STRUCTURE TYPES

-Primitive Data Types:
    -boolean
    -undefined
    -string
    -number
    -bigInt *
    -symbol *
- null
- Object
- Function
*/

// Boolean (Two Values : True and False)

let on = true;
let off = false;

// Null (Null value is empty and undefined is something that's not defined)
// Undefined (No value has been assigned. Doesn't act as an empty container either.)

let sent = "This sentence will be split into individual parts.";

const exampleArray = sent.split(" ");
console.log(exampleArray);
